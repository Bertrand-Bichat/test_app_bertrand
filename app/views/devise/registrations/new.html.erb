<div class="container">
  <!-- link back -->
  <p class="mt-5">
    <%= link_to root_path, class: "btn btn-primary" do %>
      <i class="fas fa-chevron-left"></i> Retour
    <% end %>
  </p>

  <div class="row">

    <div class="col">
    </div>

    <div class="col-12 col-md-6 my-5">
      <div class="border rounded px-4 pt-4">
        <h1 class="text-center mb-5">Créer un compte</h1>

        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>


          <div class="form-inputs">
            <%= f.input :email,
                        required: true,
                        placeholder: 'prenom.nom@gmail.com',
                        label: 'Mon email',
                        input_html: { autocomplete: 'email' } %>
            <%= f.input :password,
                        required: true,
                        label: 'Mot de passe',
                        hint: ("#{@minimum_password_length} caractères minimum" if @minimum_password_length),
                        input_html: { autocomplete: 'new-password' } %>
            <%= f.input :password_confirmation,
                        required: true,
                        label: 'Confirmer le mot de passe' %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, 'Valider', class: "btn btn-primary font-weight-bold button-text w-100 my-3" %>
          </div>
          <p class="mt-3 devise-links">Déja un compte ? <%= link_to 'Se connecter', new_session_path(resource_name), class: "text-decoration-none font-weight-bold" %> </p>
        <% end %>
      </div>
    </div>

    <div class="col">
    </div>

  </div>
</div>
